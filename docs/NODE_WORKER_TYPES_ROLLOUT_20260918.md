# 节点 Worker 功能类型选择上线记录

2026-09-18（UTC）。已部署，仅更新 Dashboard。

新增服务器页面提供全量抓取（`youtube-channel-crawl`）和增量抓取（`youtube-channel-incremental`）选择。类型随登记保存，在卡片、详情和 Worker 管理中回显。全量可登记和初始化，远程部署尚未开放；已部署节点不能通过修改登记类型切换任务。实际全量远程执行仍按 [全量方案](FULL_CRAWL_REMOTE_EXECUTION_PLAN_20260918.md) 后续实施。

## 发布内容

- 容器：`qy-newcrawler-fresh-dashboard-1`。
- 新镜像：`qy-allpachong/dashboard:node-worker-types-20260918-73c172237805`。
- 原镜像：`qy-allpachong/dashboard:node-deploy-timeout-20260917`。
- 以线上原镜像为基础，仅叠加 7 个相关源码文件；保留线上已有接单文案和执行控制路由的差异。未更新 `server.js`、基础依赖或其他服务镜像。
- 部署来源为工作区修改，Git 基线 `6da77452aeab`，源文件清单 SHA256 为 `73c1722378054ea1ce74bbdda7cd874a952c84a9ec0ca6bfef475001a4580b1f`。镜像标记为 dirty，未声称全部对应干净提交。
- 原配置、环境、挂载、网络和健康检查保留，`SKIP_SCHEMA_MIGRATION=true`。未修改数据库 schema、队列或接单额度。
- Dashboard 切换及健康/资源检查约 14.54 秒；该值不是外部页面精确中断时长。
- 当前容器标签指向的持久 Compose 文件已同步新镜像，避免下次重建恢复旧版本。

## 验证

- 工作区相关回归：25 项通过。
- 新镜像内使用生产运行时、无外网及无生产凭据的回归：21 项通过，无跳过。Python 安装器测试已在主机侧验证，未假定 Dashboard 运行镜像安装 Python。
- 隔离浏览器验证新增、保存、刷新、编辑、移动端、未开放类型部署拦截和原增量管理。
- 上线后通过真实 Dashboard 返回的页面、模块和只读接口完成浏览器验证：两个类型选项正确、原节点回显为增量且不可改类型、管理入口正常、无 JavaScript 错误。验证拦截所有非 GET 请求，没有登记测试节点。
- 线上 7 个文件的内容 hash 与已测试构建清单一致，Dashboard 健康检查通过。
- 节点登记版本部署前后均为 352。两台远程节点的部署数/连接数/就绪数/接单额度分别保持 47 和 15，共 62 个。
- 62 个远程 Worker 的 instance ID、relay boot ID 和运行版本均未变化。
- 中心执行、NATS、Rota、队列相关服务等 90 个受保护容器的 ID、启动时间、重启次数和镜像均未变化，且仍运行。
- 增量队列前后均未暂停，active 均为 62，failed 均为 992。任务结果的最近应用时间从 06:23:49.184 推进到 06:26:56.560；后一次采样最近 5 分钟应用 604 个远程任务结果。该统计用于证明持续推进，不是吞吐提升对照实验。

## 回退与证据

保留原容器 `qy-newcrawler-fresh-dashboard-1-before-node-types-20260918`，其自动重启已关闭。发布脚本在新版本健康或页面检查失败时可恢复原容器；此次未触发回退。

后续手动回退必须避开正在进行的节点管理操作，并检查上线后是否已保存新 `workerRole` 元数据。旧 Dashboard 不理解该类型，不能在已有全量登记后直接恢复旧版并继续部署 Worker；应使用保留类型校验的修复版本或先完成明确的兼容处理。

受限部署证据保存在 `runtime/node-worker-types-20260918/`，包含源码清单、镜像测试、上线前后观测和回滚配置。`.private.json` 包含容器配置，可能含凭据，不应发布。复现此次线上构建应使用该目录的实际构建源码及清单，以保留已核对的线上差异。
