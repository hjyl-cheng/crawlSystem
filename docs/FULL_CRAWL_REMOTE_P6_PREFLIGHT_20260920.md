# P6 首轮生产预检

后续更新（07:28 UTC）：用户已添加并初始化“全量采集节点”，实际 SSH/资源/网络核验通过，服务器缺失阻塞已解除。当前状态见 [专用节点环境核验](FULL_CRAWL_REMOTE_P6_NODE_ENVIRONMENT_20260920.md)。下文保留 07:07 首轮历史快照，中心发布门槛仍待完成。

采样时间：2026-09-20 07:07–07:09 UTC。状态：完成首轮中心只读盘点，**尚不具备开放灰度的条件**；未执行迁移、部署、暂停或接单变更。P5 隔离验收结果继续有效，P6 生产验收未通过。

机器证据：[fullcrawl-p6-preflight-20260920.json](../reports/fullcrawl-p6-preflight-20260920.json)。可复用的只读探针和原始证据在 `runtime/fullcrawl-p6-20260920/`；数据库会话强制只读、单语句超时 10 秒，Redis 仅执行读命令，管理接口仅查询 status/health/capacity。生产凭据没有写入报告。

## 是否需要新服务器

已登记的两台执行节点均在承担增量任务：更新节点 01 有 47 个，增量节点 02 有 15 个，合计 62 个 Worker 全部 connected/enabled/accepting。没有已登记的闲置执行节点，也没有全量节点。

因此需要准备一台**独立的全量执行服务器**。可以复用未登记的闲置服务器，不要求购买新机器；不能把现有增量部署改成全量或在其单一部署记录下混装。尚未取得该服务器地址，因此节点 SSH、硬件、磁盘、spool、镜像拉取和 TLS/WSS 路由验收待做。

建议首次单 Worker 灰度使用 Linux x86_64、2 vCPU、4 GiB 内存、40 GB SSD，保持足够空闲磁盘；若准备随后观察 5 个 Worker，建议 4 vCPU、8 GiB、60 GB SSD 起步。这是预留操作系统、Docker 镜像和日志空间的规划建议，不是经过真实频道压测的最低配置或吞吐承诺。实际配方每个全量 Worker 的容器内存上限 768 MiB，spool 配额 256 MiB，达到 80% 停止扩大并保留证据。CPU 和实际峰值仍须灰度测量。

## 中心与依赖

| 项目 | 当前证据 | 对发布的影响 |
| --- | --- | --- |
| 中心宿主 | 16 CPU；1/5/15 分钟负载约 19.1/19.9/20.4；可用内存约 5.1 GiB，无 swap | 不能据单次内存余量判定容量通过；需要复核持续 CPU/IO 压力和共享服务预算 |
| 磁盘 | 已用 88%，剩余约 99 GiB | 较历史 80% 增长；全量发布前需核实增长来源、保留空间和容量计划，不自动删除业务或 spool 数据 |
| 中心容器 | healthy；375 MiB / 768 MiB；本次快照 CPU 177%；启动以来未重启 | 全量配方需 1536 MiB / 192 PID，并计入一个本地兼容槽 |
| NATS | 两次 transport health 均 HTTP 200，pending/unacknowledged 均 0 | RPC 超时累计 15，两次采样未增长；不是当前窗口新增 15 次 |
| PostgreSQL | 39–43 连接；deadlocks 累计 6408，两次未增长 | 初次快照 3 个 Lock 等待、5 个 idle in transaction；迁移前重新检查锁，不强抢 DDL |
| Redis | evicted_keys=0；使用约 704 MiB，maxmemory=0 | 当前队列持续推进；仍需共享容量观察 |
| Rota | channel desired/ready/claimed=123；reserve=1129，minimum=32 | 当前 123 已全部 claimed，储备不是已经分配的全量槽；灰度需新增 1 远程全量 + 1 本地兼容预算 |
| 全量 schema | 五张 full_crawl 核心表均不存在 | 待按 P5 短锁迁移流程执行 |
| 全量开关 | 当前中心 deployment/execution 均未配置 | 远程全量保持关闭；原本地全量仍有 2 个 active job |

现有预算是 61 个本地 channel 槽 + 62 个远程增量槽 = 123。首次灰度若其他数量不变，应核算为 62 个本地槽（含新增兼容槽）+ 62 增量 + 1 全量 = 125，并检查自动容量控制不会覆盖该预算。`REMOTE_NODE_FULL_CRAWL_TOTAL_SLOTS=2` 包含兼容槽，不等于两个远程槽。

## 增量基线及边界

两个完整窗口：

| UTC 窗口 | 有效业务完成任务 | completed 事件 | failed 事件 | 失败事件 /（completed + failed） |
| --- | ---: | ---: | ---: | ---: |
| 06:30–06:45 | 1178 | 1180 | 76 | 6.05% |
| 06:45–07:00 | 1027 | 1027 | 76 | 6.89% |

有效业务完成按 distinct job_id、非 duplicate、ok/done、关联 run 已 done 且启用 domain 均 complete 复核。事件错误率包含重试，不能作为最终失败任务比例。第一个窗口有 2 个 completed 事件未满足业务复核条件，未计入有效任务。

队列快照：增量 waiting=54、active=62、delayed=0、failed 存量=1500。最近 30 分钟保留的 BullMQ 完成样本共 2002 个，P95 等待 174.907 秒、执行 82.777 秒。此样本窗口为 06:37:36–07:07:36，与整刻业务窗口不同；processedOn 可能受重试影响。不能把这些样本冒充逐窗口的端到端业务耗时。

已有两个窗口吞吐差异约 12.8%，且发布尚未发生；不得归因于全量灰度。正式发布紧邻操作时必须重新采集可比负载基线，补齐按窗口分位数和资源趋势。当前数据是首轮盘点，不是最终灰度放行基线。

## 版本和回退准备

- 线上中心：`qy-allpachong/remote-node-center:bug0918-01-bf933f05e0e3`，image ID `sha256:e0049b8ed64e8279a491983c5ba30076af93962dddf54c8db86172670b49d168`。原接单 selected_slots/revision、关键容器 image ID 和配置环境哈希已保存。
- 检查 7 个线上关键修复文件：6 个与工作区完全一致，`centerExecutionSupervisor.js` 的差异为 P1–P5 角色/队列参数化、全量就绪检查和恢复注入；已检查 diff，原慢恢复的逐槽执行逻辑保留。此为范围明确的核对，不能代替整个镜像部署前差异检查。
- P5 镜像源码清单 475 个文件再次核对，全部与当前工作区匹配。正式镜像分发尚未执行；P5 回环 registry 地址仍不能给远程节点使用。
- 正式 registry 服务当前存在；尚未发布全量产物、验证目标节点拉取或变更 Dashboard。完整受限配置备份和发布/回退配置在实际发布前按当时状态生成，当前哈希不是可直接还原的配置备份。

## 恢复执行顺序

1. 取得并登记专用全量服务器的地址、SSH 端口/用户名和既有密钥认证方式；选择 `fullcrawl / youtube-channel-crawl`，先做只读机器和网络检查。
2. 复核中心负载、磁盘增长、数据库锁及增量基线，落实新增兼容槽和全量槽的资源预算；必要时先解决容量问题。
3. 固定正式仓库镜像、Dashboard 发布包、受限配置备份和回退配置，完成源代码/密钥连续性/DB/Redis 身份校验。
4. 按 P5 清单迁移兼容 schema、保存并排空受影响增量、升级兼容中心、恢复原接单并观察正常推进。
5. 部署 1 个 full Worker，验证身份、网络、spool、停止回执和就绪，再开放 1 个远程全量槽。达到至少 30 分钟 / 10 个有效频道及业务发布门槛后，才进入下一档。

当前明确阻塞是没有指定的全量服务器；中心资源和发布准备也尚未达到放行状态。没有执行任何生产变更以绕过这些门槛。
