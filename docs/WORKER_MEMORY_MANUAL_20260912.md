# 2026-09-12：人工决定 Worker 数量，取消内存预算准入

用户明确要求不按“每 Worker 256 MiB + 系统预留 1.5 GiB”阻止手动新增。

- 页面保留估算与节点总内存作为普通参考文字，移除按内存计算的最大数量、剩余额度、红色错误和按钮禁用条件。
- Dashboard 删除部署前的内存准入调用及对应 SSH 方法。
- 远程安装脚本删除根据 MemTotal 拒绝部署的条件。脚本随每次页面部署自动上传，无需重启或手动修改已有节点。
- 安装数量、身份、固定镜像、已有槽位、部署幂等和接单控制仍按原机制验证。当前架构单节点最多 32 个，容器自己的 768 MiB 运行上限保持原值；本次不是调整整个执行架构的数量范围。

验证：实际安装器模拟 7.44 GiB 节点，20/23/24/32 个均通过文件写入前的校验；固定槽位配置不变。真实隔离 PostgreSQL+页面 API 集成测试覆盖部署进度、重试、扩容、并发设置覆盖保护，以及不调用旧内存准入检查。

上线镜像 `qy-allpachong/dashboard:worker-memory-manual-20260912`，仅重建 Dashboard。同步持久化部署清单；备份在私有 `runtime/remote-center-production/worker-memory-manual-20260912/`。期间增量队列保持运行，active=23、waiting=10，未触发新增 Worker 或修改接单数量。
