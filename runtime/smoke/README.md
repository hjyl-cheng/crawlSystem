# Smoke Runtime

This environment is reserved for isolated Compose verification. It must use a
unique project name, ports, networks, and volumes and must never connect to
production databases or queues.
