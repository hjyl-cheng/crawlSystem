# Encrypted Runtime Backups

Store only encrypted exports here. Encryption keys must be kept outside this
repository and outside the backup itself. Database and object-storage backups
remain separate from source releases.
