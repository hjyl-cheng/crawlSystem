# Append to the Rota project's existing Compose files.
# 40 Full Crawl + 20 incremental + 1 existing auxiliary channel-role worker.
services:
  rota-core:
    environment:
      ROTA_CHANNEL_SLOTS: "61"
