# 全量采集服务器部署检测

检测时间：2026-09-22 01:49–01:52 UTC（北京时间 09:49–09:52）。
检查方式：生产中心只读数据库查询、中心状态接口、Docker inspect，以及使用已登记主机指纹的 SSH 只读检查。未修改生产配置、容器或接单开关。

## 结论

全量采集服务器 `43.172.77.209` 部署未通过验证，当前未接单。10 个 Worker 容器已创建，8 个稳定运行，2 个持续退出并重启。

## 部署登记

- 节点：全量采集节点，`8a07de4f-a3ee-428f-a959-2aee9c7b6be7`。
- 部署：`b19c5c9a-4a09-4a61-bdb2-9b69bed02933`，模式 `full_crawl_collect`。
- 最近一次部署：2026-09-21 13:00:08–13:01:45 UTC，目标数量 10，已确认数量仍为 3，状态 `failed`。
- SSH、文件、镜像拉取、启动步骤完成；`verify` 失败，`connection` 待执行。
- 登记错误：容器检查失败，请检查配置后重试；已有部署记录和采集暂存文件已保留。
- 节点镜像摘要：`sha256:eef3acf34ec19620d0baf111388136f0d622377831075d2576c3057ac5979164`，10 个容器一致。

## 当前运行情况

| 项目 | 实测结果 |
| --- | --- |
| 容器总数 | 10 |
| 稳定运行 | full-crawl-2、4、5、6、7、8、9、10；重启次数均为 0 |
| 异常容器 | full-crawl-1：807 次重启；full-crawl-3：789 次重启（01:50:39 UTC 快照） |
| 异常退出 | 两者退出码 1，OOMKilled=false，日志反复出现 NODE_EXECUTOR_STOPPED |
| 接单 | intakeEnabled=false，allowedCount=0，ready=0，active=0 |
| 中心连接快照 | 登记 10、在线 9、离线 1；重启期间可能短暂在线，不代表容器稳定 |
| 内存 | 共 7685 MiB，可用 6400 MiB，Swap 使用 0 |
| 根磁盘 | 79 GiB，已用 6.6 GiB，可用 69 GiB，使用率 9% |
| 暂存目录 | /var/lib/qy-node/spool 共 72 KiB |
| 单 Worker 资源限制 | 768 MiB、0.5 CPU |

两个异常容器在相隔约 65 秒的多条日志中重复报告 `NODE_EXECUTOR_STOPPED`。该错误是已确认的退出信号，底层触发原因尚未定位；不能仅凭该错误判定为内存、网络或任务数据问题。

## 中心及容量

- 中心 `qy-remote-node-center` 和管理面板均为 healthy，重启次数为 0。
- 全量控制器运行中，重启次数为 0；该容器未提供 Docker 健康检查状态。
- 中心全量部署、执行开关均为 true，但节点接单开关为 false。
- 线上中心版本：`qy-allpachong/remote-node-center:task-stop-pkey-20260921`。
- 线上配置 `REMOTE_NODE_FULL_CRAWL_TOTAL_SLOTS=6`；直接读取运行容器源码确认仍执行 `maxSlots: totalSlots - 1`，即远程最多 5 个槽位，另保留 1 个兼容槽。
- 工作区动态容量文档和实现已取消此固定上限，但该逻辑尚未体现在当前运行的中心版本中。目标 10 个 Worker 与线上 5 个远程执行槽位限制不一致。

## 采集执行记录

- 该节点全量 execution 记录共 26 条，均在检查时刻过去 24 小时内；最近一条为 2026-09-21 12:15:16 UTC。
- 全量 capability 的任务记录：applied 5，failed 21。
- close_fetch 阶段共 5 条，均已 applied；最近应用时间为 2026-09-21 12:20:38 UTC。
- 上述是远程全量执行及阶段记录，不等同于最终业务发布成功数。

## 后续处理顺序

1. 定位并修复 full-crawl-1、full-crawl-3 的 NODE_EXECUTOR_STOPPED 循环退出原因，保留暂存与恢复证据。
2. 核对并统一中心线上容量策略与 10 个 Worker 的部署目标。
3. 重新执行部署验证，使部署登记数量与实际健康数量一致。
4. 验证通过后，再按业务安排恢复接单并观察真实任务完成情况。
