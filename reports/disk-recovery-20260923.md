# 磁盘扩容后恢复记录

执行日期：2026-09-23（UTC）。用户已明确授权顺序修复及重启。中心自 04:35:33 恢复后持续处理任务；05:17 起 85/85 个远端 Worker 已连接。最终采样数值见文末及 `runtime/disk-recovery-20260923/`。

## 恢复结果

- 磁盘扩容已生效：999 GB，总使用率 81%，05:17 可用约 193 GB。
- 47 个主增量 Worker 全部连接并可接单；10 个全量 Worker 全部连接、就绪，当前没有新全量工作，处于空闲。
- node02 的 8 个、备用节点的 20 个 Worker 全部连接，原有接单关闭状态保留。
- 增量任务持续实际入库，05:35 最终采样自中心恢复以来已有 1,205 条 applied。不能将全量 Worker 就绪等同于已完成新的全量任务。
- Finalize 的磁盘相关失败由现有后台恢复流程逐步消化；05:17 等待、执行中、失败均为 0。
- 历史事件通过正式幂等接收接口补齐；既有序列等待解除，并进一步补齐尚未触发后续等待的潜在历史缺口。
- NATS 结果流正常消费和确认，未再观察到原消息块写入错误。

## 执行顺序与证据

1. 备份原配置与待发布事件。499 条待发布事件信封及线上纯解析全部通过，Redis 持久化状态正常。
2. 03:47:43 启动已退出的原 crawler-outbox-publisher 容器，原 499 条积压清空。
3. NATS 持续报 `error opening msg block file ... no such file or directory`。停止后备份持久目录，启动原容器；NATS 自动恢复损坏的流索引。其 IP 变化后校验并平滑重载 Nginx，远端 WSS 独立收据查询成功。
4. 发现结果流序号归零而消费位点仍停在 1,048,651。确认没有未确认消息并备份消费者后，仅重建消费者，保留消息流。
5. 中心原执行循环持续报 BullMQ 锁续期失败。用户授权重启后受控重启中心；首次启动遇旧兼容路由 `LEASE_CONFLICT`，停止失败启动，等待原租约自然到期，再于 04:35:33 成功启动。没有直接修改 Rota 租约。
6. 远端旧 WSS 连接未自动恢复。按单节点、每批最多 3 个已断连且无有效任务租约的 Worker 备份并重启；每批确认全部重新连接且中心/NATS 正常，才继续下一批。保持接单配置。
7. 部署 Feature ingest 的最小契约修复：旧基线没有完整处置记录、且详情抓取失败数非零时，Discovery 必须为 partial。实际线上模块与首版修复版仅 `events.py` 的这一条件不同；配置、数据库身份和挂载一致。
8. 全量 `full-crawl-9` 两次重启仍无法心跳。临时非暂停断点捕获到 `WORKER_PREVIOUS_EXECUTION_UNSETTLED`，调试接口和断点均已关闭。其旧国家复核交接任务 `504f9c38-3132-40b9-be90-fbe8c6b5f907` 的后续尝试已成功、Run/detail/fetch 均完成、candidate accepted、网络全部 retired 且 in_flight=0。保存完整证据，并在事务内再次锁定验证后调用现有 `RemoteNodeStore.cancel` 关闭旧传输记录。未标记结果成功、未删除批次或修改业务完成状态；该 Worker 随即自动恢复连接。
9. Finalize 生产者也缺少同一完成条件。以其正在运行的精确提交 `ec3aeb0` 创建隔离工作树，仅加入 2 行生产逻辑及测试，构建新镜像。等待 Finalize 队列自然排空后，正常关闭旧进程，以原配置启动新容器，保留旧容器用于回滚。
10. 唯一剩余 Finalize 失败源于历史数据库关闭错误。备份后通过 BullMQ 正式 retry 重试；系统判定 `finalize_source_revision_stale` 并安全跳过，任务自然进入 completed。
11. 最终对账发现 2 条增量事件被首版消费者修复误拒绝：失败详情已经在完整处置记录中终止排除，生产者正确判为 complete。新增真实模式回归，先复现失败，再将失败数条件限定于缺少处置记录的旧格式；完整账本继续以未解决项及阻塞项判断完成。最终消费者版本为 `314e68e`，保持原始事件不变；两条事件重新纯校验通过并经正式接口补送。

## 历史事件恢复

- 第一轮导出 425 条可见缺口的前序事件；旧线上解析器仅接受 2 条，修复版 425/425 通过。
- 先重放 10 条验收，再每 10 条一批通过 `/v1/crawler-observations` 提交，保留原 event_id、payload 和 Idempotency-Key。425 条全部 applied，自动解除原 432 条等待。
- 恢复期间新增量暴露另外 4 条更早的前序缺失；4 条均经同一接口应用并解除等待。
- 进一步以只读查询检查“中心恢复前已 published、但收件箱没有 event_id”的记录，发现 790 条。纯解析 790/790 通过；首批 10 条应用后，剩余 780 条均恰好是对应检查点的下一条，继续按 10 条批次恢复，790 条全部 applied。
- 总计 1,221 条不同历史事件全部 applied，累计自动解除 437 条后续等待。
- 未直接修改事件内容、特征状态或检查点；未删除旧失败任务记录。`feature-recalc` 保留的历史 failed 数不能等同于当前待处理积压。

## 发布与验证

| 角色 | 原版本 | 修复版本 |
| --- | --- | --- |
| feature-ingest | `pachongsys-5b535b8` | `pachongsys-314e68e-disk-recovery` |
| worker-finalize | `pachongsys-ec3aeb0` | `pachongsys-2565ce6-disk-recovery` |

- 消费者最终提交：`314e68e`，前序提交 `3ad22ba`。历史 334 条样本已验证；最终累计 1,221 条原始恢复事件全部通过纯解析。Feature Engine 最终 216 项通过、17 项跳过、102 子测试通过，源码 verify 通过。
- 消费者隔离实例通过生产数据库 readiness，零重启；线上替换后 healthy、零重启。
- 生产者提交：`2565ce6`，父提交就是线上 Finalize 的 `ec3aeb0`；提取的线上 src 与父提交完全一致。新测试先复现错误 complete，再验证修复后 partial。58 项契约/初始观测/Finalize 回归通过，构建后的实际镜像在断网容器中重复运行同组 58 项全部通过，源码 verify 通过。
- 主工作区原有改动保留，并在 Feature Engine 的现有修改上追加完整处置记录兼容修复及测试；其他用户文件未覆盖。没有将源码复制进运行容器；远端采集镜像及中心镜像未升级。

## 备份、回滚与边界

- 本地证据：`runtime/disk-recovery-20260923/`。完整容器配置和事件快照放 `private/`，目录权限 0700；配置快照 0600。
- NATS 停服一致性备份：`private/nats-data-before.tgz`。原状态已损坏，不应直接恢复覆盖正常运行的数据。
- 远端备份目录：`/var/lib/qy-node/backups/disk-recovery-20260923/`，保存容器配置和存在的增量 spool；所有原 spool 保留。
- Finalize 旧容器：`qy-newcrawler-fresh-worker-finalize-1-before-disk-recovery-20260923`，保持停止状态。应用回滚应先正常停止新版本，再恢复旧镜像及原规格；不回退数据库或重放旧 spool 覆盖新执行。
- Feature ingest 原配置与镜像保存在 `private/feature-ingest-before.json`；恢复部署时保留原镜像摘要。Compose 重建该角色时也清除了三个带相同 service 标签的旧停止状态 ingest 容器，未删除卷、镜像或数据库数据。
- 两次较大范围操作被自动审批拒绝：中心批量中止执行、跨四台机器扩大重启。它们均未执行；前者在用户明确授权后改为受控重启，后者改为逐台、每批 3 个并逐批验收。
- 系统恢复不代表所有历史业务失败都变为成功。仍可见业务/代理预算耗尽、代理传输、租约竞争及少量唯一约束错误；其他业务队列有既有失败记录和内容富化历史告警，本次没有解除预算限制或做无差别重试。


## 最终验收（05:35 UTC）

- 85/85 Worker 在线；主增量节点 47 个运行中，全量节点 10 个就绪且空闲；两台备用节点合计 28 个保持 standby。
- 中心连续运行约 60 分钟；自恢复以来 1,205 条增量任务 applied，最新完成时间 05:35:36 UTC。
- 1,221 条不同历史事件全部 applied；累计解除 437 条后续等待。最终对账中，创建超过 5 分钟、已发布但未进入收件箱的事件为 **0**；inbox 非 applied 为 **0**。
- Finalize waiting/prioritized/active/failed 全为 **0**。
- outbox 采样有 2 条当时刚产生的 pending；feature-recalc 有 1 条 active，waiting/delayed 为 0。这是持续采集的新事件流转，不能宣称实时队列永久为空。
- feature-recalc 保留历史 failed 记录 3,687 条；没有为了让计数归零而删除记录，实际缺失投递已按数据库对账补齐。
- 全部受检容器没有 unhealthy 状态；数据库、对象存储、API 健康检查通过。NATS 两个结果流 pending/unacknowledged 均为 0。
- 主增量接单和任务执行持续变化，存在代理传输、预算及执行竞争相关业务失败；本次恢复已消除磁盘写入故障和事件缺口，没有保证每个外部采集任务都成功。
