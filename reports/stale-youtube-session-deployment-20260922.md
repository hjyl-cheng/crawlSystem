# 旧 YouTube 会话恢复线上部署验收

目标节点：`54a7cdd3-eb9c-4713-8d2f-21f4a5279de0`。
目标槽位：10、12、16、19、20、26、28、29、31、34、50。
部署时间：2026-09-22 12:23–12:28 UTC。

## 部署

基于现有生产 Worker 镜像 `pachongsys-69b717c` 构建了只覆盖本次恢复修复的镜像，发布摘要为：

`newcrawdashboard.137-175-93-199.nip.io/qy-node-incremental@sha256:242f4609f406b6476d8192a9f4c0b327c50932e66a04a544b6009e7eb20391d3`

槽位 10 先行试部署，随后完成其余 10 个槽位。每个槽位都保留了原 Compose、容器检查结果和 spool 压缩备份；没有清空 spool，也没有修改中心任务或手工结束租约。远端持久 Compose 的 11 个目标服务均已切换到该摘要，容器均 running，重启数为 0。

批量脚本最后的环境数组严格相等断言在槽位 20 处停止，但该断言发生在所有容器替换完成之后。后续逐容器核验确认 11 个容器均使用新摘要、运行中且重启数为 0；Compose、挂载和运行时配置保持目标服务原值。

## 现场证据

部署前，11 个槽位均有旧 `youtube-session.json`，其 request 对应的中心任务已经是更高代次，且网络绑定为 retired。中心同时显示 11 个新的 pending 任务等待目标槽位。

部署后逐槽位比较结果：

- 中心连接 `alive=true` 且 `accepting=true`：11/11。
- 新镜像摘要：11/11。
- 容器重启数：11 个均为 0。
- 旧会话归档增量：11/11，每个槽位恰增加 1 个 `.stale` 文件；总归档数增加 11。
- 旧会话 request 仍作为活动会话：0/11。
- 新会话活动文件：随新任务执行出现并正常消失，属于当前任务，不是旧代次回放。

槽位 10 的 canary 观察中，旧会话归档后中心看到新实例保持 alive/accepting，并已有 3 个新代次任务完成为 `applied`。全量观察窗口内，各目标槽位均持续 ready，任务 applied 计数继续增长；槽位 12 在最后一次采样新增 1 个 applied 任务。

## 测试

本地源码恢复/心跳/spool 回归测试全部通过。部署镜像内的相关 runtime 测试 31 项通过；另外 2 项因测试容器未挂载 `collectingWorkload.js` 和外部 whole-channel fixture 而无法在该最小镜像测试环境运行，不属于本次修改路径。canary 的真实远程运行验证通过。

证据目录：`runtime/stale-youtube-session-20260922/`。其中只保留脱敏的状态、哈希、差异和测试输出；registry 凭据未写入工作区。

## 当前状态

11 个目标 Worker 已上线修复版本并继续接收新任务。旧会话已一次性归档，未删除；中心和 Worker 均未执行人工任务状态修改。回退时应逐槽位暂停/收尾后使用对应的 Compose 与 spool 备份，不要恢复旧活动会话文件。
